# Thirteenth Meeting - 07/08

## Outline

- Showed work done on the ML pipeline worked on from the past week.
- Feedback was positive, on infrastructure in place, however output can be more interesting in terms of its use-case. Will develop a new model for a different use case that can be more beneficial for users who want to use the program.
- Other relevant use cases that were discussed, include face-detection (one) and classification of mnist data (two). Both use cases will be looked into and possibly developed.
- Discussed UI implementation, decided on a Javascript backend, and React/HTML+CSS front end to visually display. This will be looked into and started during the week.
- Discussed parameter/hyperparameter tuning when ('initially') evaluating models.
- Decided on when tuning ML that it will follow a 'Hill Climbing' approach. Changing value's (parameters) one at a time, instead of checking all possible combinations (this can be done later instead).

## What to do before next week

- Research into default parameters for classification/regression. This will help for later evaluation.
- Implement better use-case model -> either one or two or BOTH!
- Research into default parameters -> will justify evaluation process.
- Continue with UI development (once ml_pipeline is completed).
